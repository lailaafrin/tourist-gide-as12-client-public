import React from 'react';
import { Helmet } from 'react-helmet-async';

const Blogs = () => {
    return (
        <div>
            <Helmet>

                <title>Blogs</title>
            </Helmet>
            Blog
        </div>
    );
};

export default Blogs;